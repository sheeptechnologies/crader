# ==========================================
#  Crader Configuration
# ==========================================

# [REQUIRED] Database Connection
# Connection string for the PostgreSQL database (must include pgvector extension).
# Format: postgresql://<user>:<password>@<host>:<port>:<dbname>
DB_URL=postgresql://user:password@localhost:5432/crader_db

# [OPTIONAL] OpenAI API Key
# Required only if using OpenAI for embedding generation.
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx

# [OPTIONAL] Repository Storage Path
# Local path where cloned repositories are stored.
# Defaults to ./sheep_data/repositories if not set.
# In production/docker, this should be a persistent volume.
REPO_VOLUME=./sheep_data/repositories

# [INTERNAL] Opentelemetry / Tracing (Optional)
# If you are using an OTel collector, configure these:
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
# OTEL_SERVICE_NAME=crader-indexer

# ==========================================
#  Debugger / Development Tools
# ==========================================

# [OPTIONAL] Debugger Database URL
# Overrides DB_URL specifically for the `debugger/server.py` tool.
# Defaults to postgresql://sheep_user:sheep_password@localhost:5433/sheep_index
# SHEEP_DB_URL=postgresql://sheep_user:sheep_password@localhost:5433/sheep_index

# [OPTIONAL] MIGRATIONS Database URL
# Used by Alembic (db/env.py) to run migrations.
# Defaults to postgresql://sheep_user:sheep_password@localhost:5433/sheep_index
# SHEEP_INDEX_DB_URL=postgresql://sheep_user:sheep_password@localhost:5433/sheep_index
